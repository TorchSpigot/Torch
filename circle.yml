machine:
  java:
    version: oraclejdk8
  dependencies:
    cache-directories:
      - "./work/Paper/work/1.11.2/net"
  notify:
    branches:
      only:
        - master

test:
  override:
    - chmod +x scripts/build.sh
    - ./scripts/build.sh --init
  post:
    - yes|cp -rf ./Torchpowered-1.11.2.jar $CIRCLE_ARTIFACTS
