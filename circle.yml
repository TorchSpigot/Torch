machine:
  java:
    version: oraclejdk8
  notify:
    branches:
      only:
        - master

test:
  override:
    - chmod +x scripts/build.sh
    - ./scripts/build.sh --init
  post:
    - yes|cp -rf ./TorchPowered-*.jar $CIRCLE_ARTIFACTS
